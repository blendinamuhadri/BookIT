const FooterComponent = () => (
    <footer>
        <span className="my-footer">
            BookIT &copy; {new Date().getFullYear()}
        </span>
    </footer>
);

export default FooterComponent;
